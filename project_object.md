# Project Objective: mr.data

## 核心目标

mr.data 是一个**可长期演进的人格子系统原型**。它的核心目标是让 AI 助手能够：

1. **稳定地扮演一个虚构角色**（默认是《星际迷航：下一代》中的 Data），并在多轮、多会话对话中保持性格一致。
2. **维护长期记忆**：包括角色自身的性格素材、与用户的过往对话（情节记忆），以及从网络获取的世界知识。
3. **通过离线归因持续优化性格**：分析已关闭会话的对话结果，把成功或失败归因到具体性格维度，强化有效维度、淘汰失败维度。

一句话概括：**让 LLM 角色拥有“人格 + 记忆 + 自我修正”的能力**。

---

## 核心概念

### 1. 人格（Persona）

角色由**固定身份**、**可演进的性格维度**和**人格素材**共同定义。

- 固定身份：姓名、角色定位、基础人设，长期不变。
- 性格维度：描述角色特质的条目，随对话结果成功或失败而被强化或弱化；核心维度受保护，不会被自动淘汰。
- 人格素材：示例台词、角色经历、归因证据，用于塑造回复风格和行为倾向。

### 2. 记忆（Memory）

三类记忆共同支撑上下文：

- **情节记忆**：过往会话的完整对话，用于回忆“之前聊过什么”。
- **世界知识**：从网络获取并经沉淀的事实性资料，用于回答需要外部信息的问题。
- **近期上下文**：当前会话最近若干轮对话，用于保持短期连续性。

### 3. 在线对话

每轮对话按“理解意图 → 检索相关素材 → 组装上下文 → 生成回复”的流程执行：

- 先根据用户输入选择本次应起作用的性格维度；
- 决定是否检索网络资料，并检索人格素材与记忆；
- 将角色设定、素材、历史上下文按清晰结构交给模型；
- 生成带参考来源说明的回复。

生成时要保证：**人格风格优先，事实依据其次**。

### 4. 离线归因

对已经结束的会话进行复盘：

- 将会话对话按时间顺序整理为完整上下文；
- 让模型分析哪些性格维度促成了成功或失败；
- 更新维度权重，并提取关键证据回写到人格素材中；
- 反复失败的非核心维度会被自动淘汰；
- 最终把会话内容沉淀为情节记忆。

### 5. 可解释性

回复应当能说明自己参考了哪些素材。参考信息包括来源标识和简短摘要，便于后续追溯和展示。

---

## 设计原则（对齐用）

1. **人格优先于事实**：回复风格必须符合角色设定，事实只作为内容依据。
2. **结构数据是权威来源**：持久化存储负责记录真实关系，向量检索只用于语义召回。
3. **会话边界清晰**：活跃会话在线处理，已关闭会话离线复盘。
4. **可配置与可降级**：核心功能应尽量可开关，并在模型接口不支持时平滑降级。
5. **原型优先**：先验证机制正确性，再考虑规模化和生产级替换。

---

## 非目标（避免偏离）

- 不是通用聊天机器人或开放域问答系统；重点是**角色一致性与性格演化**。
- 不是多智能体系统；当前只有一个角色主体，外加可选的搜索工具。
- 不追求生产级高可用；持久化、扩展性、真实用户反馈闭环是后续增强方向。
