# Project Objective: mr.data

## 核心目标

mr.data 是一个**可长期演进的人格子系统原型**。它的核心目标是让 AI 助手能够：

1. **稳定地扮演一个虚构角色**（默认是《星际迷航：下一代》中的 Data），并在多轮、多会话对话中保持性格一致。
2. **维护长期记忆**：包括角色自身的性格素材、与用户的过往对话（情节记忆），以及从网络获取的世界知识。
3. **通过离线归因持续优化性格**：分析已关闭会话的对话结果，把成功或失败归因到具体性格维度，强化有效维度、淘汰失败维度。

一句话概括：**让 LLM 角色拥有“人格 + 记忆 + 自我修正”的能力**。

---

## 核心概念

### 1. 人格模型

- **固定身份（`fixed_identity`）**：角色的姓名、角色定位、基础人设。稳定不变，注入到每次对话的 system prompt 中。
- **性格维度（`personality_dimensions`）**：描述角色特质的一句话条目，例如“我相信逻辑高于情绪”。每个维度有成功/失败计数，并支持 `core` 标记。`core=True` 的维度不会被离线归因自动失效。
- **人格素材（`personality` 向量库）**：包括原始示例台词（`line`）、角色经历事件（`event`）、离线归因提取的证据（`evidence`）。按“场景上下文 embedding + agent 台词 utterance”方式存储，检索时匹配场景，注入时只取 utterance。
- **证据与事件**：离线归因从对话中提取关键片段作为证据写回人格库，并标记证据与性格的关系（体现/强化/违背/修正）。

### 2. 记忆模型

- **情节记忆（`memories`，`source_type="dialogue"`）**：会话关闭后由离线归因写入的整段对话。在线检索命中时递增 `recall_count`，长期未召回的会被清理。
- **世界知识（`memories`，`source_type="web"`）**：网络搜索结果被压缩后以 URL 哈希作为稳定 id 写入，作为全局知识跨会话共享。
- **近期上下文（`DialogueState.messages`）**：当前会话最近 N 轮对话，从 Postgres 的 `dialogue_logs` 加载，包含隐藏的 `inner_monologue` 和 `references`。用于弥补向量检索对“刚刚说过什么”的延迟。

### 3. 在线对话流水线

每轮对话由 LangGraph 编排：

1. 加载人格、用户身份、活跃维度。
2. 根据用户输入**选择本次应起作用的性格维度**。
3. **Think**：生成检索查询、内心独白、是否联网搜索。
4. 可选 **Web RAG**：搜索 → 网页正文提取 → LLM 相关性过滤。
5. 检索人格素材与情节记忆（可再经过相关性过滤）。
6. **组装上下文**，严格区分消息角色：
   - `system`：角色/身份/核心性格 + 如何使用后续素材的指引 + 输出格式要求。
   - `assistant`：分类上下文素材（`<personality>` / `<memory>` / `<messages>` / `<web>`）。
   - `user`：当前用户输入。
7. **结构化生成**：输出 `AssistantReply`（`text` + `references`），reference 携带来源 id 与一句话总结。
8. 记录对话日志，并把 assistant 的 `inner_monologue` 与 `references` 作为隐藏 metadata 写入 `dialogue_logs`。

上下文总量受 `llm_context_token_limit` 约束，超限时按优先级压缩或截断。

### 4. 离线归因流水线

- 只处理**已关闭会话**的未处理对话。
- 按时间顺序拼接完整 transcript，并注入基础人设、当前维度、历史人格素材、助手思考过程。
- LLM 判断哪些维度促成成功/失败，更新计数并写入 `adjustment_logs`。
- 提取证据片段和事件摘要，写入 `personality` 向量库，并在 `dialogue_vector_refs` 记录反向引用。
- 非核心维度失败次数达到阈值后自动失效，并清理对应向量与引用。
- 最终把会话对话作为情节记忆写入 `memories`。

### 5. 可解释性与溯源

- 每条回复都附带 `references`，标明参考了哪些人格/记忆/网络素材。
- CLI 支持 `--show-references` 显示参考来源。
- Web 资料使用 URL 哈希作为长期稳定 id，保证 `dialogue_vector_refs` 与 `memories` 中的 doc id 一致、可追溯。

---

## 设计原则（对齐用）

1. **人格优先于事实**：网络资料和记忆用于回答问题，但组织语言时必须符合人格素材和核心性格。
2. **结构数据是真相来源**：Postgres 保存身份、维度、会话、对话日志；向量库用于语义检索，关键关系通过 `dialogue_vector_refs` 等表反向关联。
3. **会话边界清晰**：在线只处理活跃会话；会话关闭后才进入离线归因和长期记忆写入。
4. **可配置与可降级**：网络搜索、网页提取、相关性过滤、参考来源显示等均可开关；结构化输出在无法使用 `parse` API 时自动降级为普通 chat + JSON Schema。
5. **原型优先**：项目使用嵌入式 PostgreSQL 和 Chroma 降低运行成本，但设计上保留了切换到外部 PG/Qdrant 的余地。

---

## 非目标（避免偏离）

- 不是通用聊天机器人或开放域问答系统；重点是**角色一致性与性格演化**。
- 不是多智能体系统；当前只有一个角色主体，外加可选的搜索工具。
- 不追求生产级高可用；持久化、扩展性、真实用户反馈闭环是后续增强方向。
