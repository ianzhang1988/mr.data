
# 已完成的改进项

1. ✅ 增加 `sessions` 表；CLI 支持 `/newsession` 切换会话；`dialogue_logs` 与 `adjustment_logs` 增加 `session_id`；离线归因按已关闭会话处理。
2. ✅ LangGraph 在线流水线增加 Web Search RAG 节点（默认开启，基于 DuckDuckGo）。
3. ✅ 引入 `pgembed` 作为默认数据库（测试使用临时目录，日常使用持久化目录），并基于它完成功能测试。
4. ✅ 离线归因改为**会话级 transcript**：按时间顺序拼接 `user` / `assistant` 对话，避免顺序混乱。
5. ✅ 离线归因提示词注入**基础人设、当前性格维度、历史人格向量库素材**作为上下文。
6. ✅ 归因时同步提取**关键证据片段**写入性格向量库（`source_type="evidence"`），并标记与基础性格的关系（`relation_to_personality`）；同时在 `dialogue_vector_refs` 记录反向引用。
7. ✅ 维度失败次数达到 `MR_DATA_FAILURE_THRESHOLD` 时自动**标记失效**，并清理 `personality` 向量库中对应的证据文档与 Postgres 引用记录。
8. ✅ 性格向量库改为**场景上下文 embedding + agent 台词 utterance**：`PersonalityEvent` 新增 `context` 与 `speaker`；Chroma 存储时嵌入完整场景，检索返回时只取 `metadata.utterance`。
9. ✅ **结构化日志系统**：新增 `src/mr_data/logging.py`，JSONL 输出到 `./logs/mr-data.log`，支持滚动；在线对话记录检索查询与内心独白，离线归因读取思考过程并纳入提示词。
10. ✅ **网页正文提取工具**：新增 `PageExtractor`（`trafilatura` + `requests/BeautifulSoup` fallback），接入 LangGraph，在 web search 后提取页面正文。
11. ✅ **固定核心性格标记**：`personality_dimensions` 增加 `core` 列；核心维度不会被离线归因自动失效，保持角色稳定性。
12. ✅ **在线核心性格选择**：`DialogueGraph` 在 `_think` 前增加 `_select_dimensions` 节点，由 LLM 根据用户输入选出最应起作用的性格维度，并注入 `_think` 提示词。
13. ✅ **Web 检索条件分支**：`retrieve_web`、`extract_web_pages` 改为 `conditional_edges`，根据配置和中间结果动态跳过。
14. ✅ **网页资料 LLM 相关性过滤**：`extract_web_pages` 后可选通过 LLM 逐文档判断与用户输入的相关性，保留相关文档。
15. ✅ **网络资料写入世界知识记忆**：`_log_dialogue` 把 `web_docs` 写入 `memories` 向量库，附带 `source_type=web`、URL、标题、检索时间、查询等 metadata。
16. ✅ **默认人格改为 Data**：`PostgresStore.seed()` 默认人格原型改为《星际迷航：下一代》中的 Data；新增 `PersonalityPack`/`PersonalitySampleLine` 模型与 `personality_loader`，支持从 `data/personalities/*.json` 加载人格，代码常量作为兜底；`mr-data ingest` 从人格包读取示例台词。
17. ✅ **Chroma 高级 Embedding**：`personality` 集合改用 `fastembed` + `nomic-ai/nomic-embed-text-v1.5` 并截断至 512 维，`memories` 集合改用 `BAAI/bge-base-zh-v1.5` 768 维；代码中自动添加 Nomic/BGE 所需的 query/document 前缀；旧集合维度不一致时自动重建。
18. ✅ **用户身份设定**：新增 `user_identities` 表，支持保存多个用户身份；seed 时写入 Picard（默认、受保护）与普通用户（受保护）；`DialogueGraph._assemble_and_generate` 从数据库读取当前默认身份并注入 system prompt；CLI 新增 `mr-data identity list/add/edit/delete/select` 管理身份。
19. ✅ **交互式帮助命令**：`mr-data chat` 中输入 `/help` 或 `/?` 可显示当前 slash 命令、启动选项及顶层 CLI 命令。
20. ✅ **think 节点结构化决策**：`DialogueGraph._think` 使用 `ThinkDecision` 结构化输出，生成 `personality_query`、`memory_query`、`needs_web_search`、`search_query` 与 `inner_monologue`；web 分支仅由 think 决策和 `enable_web_search` 单一开关控制，`retrieve_web` / `extract_web_pages` / `filter_web_docs` 作为整体流水线依次执行。
21. ✅ **离线对话记忆与 recall 计数**：离线归因后将对话日志写入 `memories` 向量库（`source_type=dialogue`），记录 `recall_count`；在线检索命中对话记忆时递增计数；新增 `prune_stale_dialogue_memories` 清理长期未召回的旧对话记忆。
22. ✅ **统一结构化输出与 LLM 适配降级**：`LLMClient` 新增 `structured_chat`，先尝试 OpenAI `parse` API，失败时自动降级为普通 chat + JSON Schema prompt + 解析；`_select_dimensions`、`_filter_web_docs`、离线归因统一改为结构化输出，提升不同 LLM 端点的适配性。
23. ✅ **多源 Web 搜索与失败降级**：新增 `SearchProvider` 协议与 `search_providers.py`，支持 DuckDuckGo、SearXNG、Brave、Bing、Google CSE、百度、360；`WebSearchTool` 改为按配置顺序调用，失败自动降级；新增 `web_search_providers` 等配置项。
24. ✅ **记忆相关性过滤节点**：`_retrieve_memories` 后增加可选 `_filter_memory_docs` 节点，由 LLM 批量判断记忆与用户输入的相关性，扩大 `memory_retrieval_top_k` 后自动精简记忆内容；新增 `enable_memory_relevance_filter` 配置。
25. ✅ **`_assemble_and_generate` 结构化输出与参考来源**：`AssistantReply`/`ReplyReference` 模型定义回复文本与参考列表；生成时要求 LLM 输出每条参考的 id、source_type 和一句话 summary；CLI 新增 `--show-references`/`show_references` 配置控制是否显示参考来源。
26. ✅ **Web 搜索结果长期稳定 id**：`_to_doc_format` 改用 URL SHA256 哈希作为 doc id；新增 `ChromaStore.upsert_memory`，`_log_dialogue` 写入网络资料时使用稳定 id 并作为全局知识（`session_id=""`），避免 `web:0/web:1` 每轮重置导致的冲突。
27. ✅ **DialogueState 保留最近 N 轮 messages**：`dialogue_logs` 新增 `metadata` JSONB 字段，用于保存 assistant 回复的 `inner_monologue` 和 `reply_references`；每轮启动时从 Postgres 加载最近对话构造 `DialogueMessage` 列表；`DialogueState` 新增 `messages` 字段与 reducer；配置项 `dialogue_state_message_turns` 默认 10。
28. ✅ **`_assemble_and_generate` Token 预算与提示词结构**：引入 `tiktoken` 实现 `TokenCounter`；新增 `llm_context_token_limit`（默认 30K）与 `tokenizer_model` 配置；按优先级组织 prompt section：system / identity+核心性格 / 用户输入 / personality / memory / messages / web / format；超限时按优先级从低到高压缩，低优先级内容调用 LLM 摘要或硬性截断。进一步将上下文材料从 system prompt 拆分到 `assistant` role 消息中，system 首段明确指引 LLM 如何使用人格/记忆/对话/web 素材；assistant 消息中每类素材使用 XML 标签（`<personality>`/`<memory>`/`<messages>`/`<web>`）包裹，便于模型识别分类。
29. ✅ **`_filter_web_docs` 逐文档判断与段落摘取**：新增 `WebDocExtraction` 模型与 `online/web_filter.py`；每条 web 文档单独 LLM 调用，先判断相关性，相关则摘取/简化与用户输入相关的内容替换 `page_content`（metadata 标记 `llm_extracted`），不相关丢弃；单文档异常保留原文档，全部丢弃时 fallback 保留全部；新增 `enable_web_doc_extraction` 配置（默认开启）；移除旧的批量过滤模型 `WebRelevanceItem`/`WebRelevanceFilterResult`。
30. ✅ **graph.py 帮助函数拆出**：新增 `online/prompt_assembly.py`，移入 `PromptSection`、`wrap_section`、`build_messages` 与 `PromptAssembler` 类（`fit_sections`/`_compress_to_budget`/`_compress_text`）；graph.py 保留 `DialogueState`/`ChatResult`/reducer 与节点组装逻辑。
31. ✅ **对话记忆改为离线按会话分段写入**：删除 `_log_dialogue` 中每轮无 metadata 的逐条 `add_memory`（野数据，无 `source_type` 标签、不参与 recall 计数与清理）；`attribution.py` 新增 `chunk_dialogue_logs`（按字符预算分段、段间保留 overlap 行）与 `_persist_session_memories`，离线归因后按会话整体分段写入记忆向量库，metadata 含 `source_type="dialogue"`、`chunk_index`、首尾 `dialogue_log_id`、`recall_count` 等完整标签；新增配置 `memory_dialogue_chunk_chars`（默认 1200）与 `memory_dialogue_chunk_overlap_lines`（默认 2）。
32. ✅ **删除 DialogueState reducer（`_merge_docs`/`_merge_messages`）**：reducer 并集/拼接语义与节点 `{**state, ...}` 返回方式冲突，导致 web 文档过滤/提取结果被还原、messages 每节点翻倍；图为线性无并行分支，reducer 无必要，4 个字段改为普通 overwrite 语义。
33. ✅ **归因失败不再误标记已处理**：`_attribute_session` 异常时返回 `None`（而非空结果），`run()` 检测到失败后 `continue`，不写对话记忆、不 `mark_dialogue_processed`，留待下次离线运行重试。
34. ✅ **选中维度在 assemble 与 log 中生效**：`_assemble_and_generate` 的 `dim_text` 与 `_log_dialogue` 的 `insert_dialogue_dimension_refs` 改用 `selected_dimension_ids` 过滤（空时回退全量），不再使用全量维度。
35. ✅ **测试共享 PG 状态隔离**：`tests/conftest.py` 新增 function 级 fixture `reset_pg_state`（依赖 `pg_available`），每个 PG 测试前对 8 张业务表 `TRUNCATE ... RESTART IDENTITY CASCADE` 后重跑 `seed()`，保证维度 id 恒为 1-5、种子数据每用例还原；4 个 PG 测试文件（test_e2e/test_user_identity/test_attribution_failure/test_graph_state）以 `pytestmark = usefixtures` 启用；顺带删除两处肇事还原代码（`test_select_dimensions_uses_core_flag` 末尾把 dim2 `core` 错误"还原"为 FALSE——seed 原值是 TRUE；`test_user_identity_crud` 末尾手动还原默认身份）。已验证 purge 测试先跑再放全文件、各文件单独运行、全量连跑两次均无顺序依赖与跨运行残留。
36. ✅ **删除废弃脚本 `scripts/ingest_personality.py`**：硬编码人格重设计前的旧维度/台词，功能被 `mr-data ingest`（走 `load_personality_pack`）完全覆盖，全仓库零引用，运行反而会写入过时数据与重复向量。
37. ✅ **统一 LLM 结构化输出调用**：`LLMClient` 三个结构化方法合并为统一核心入口 `structured_chat(messages, response_format, temperature)`（内置降级），`chat_structured(system_prompt, user_prompt, ...)` 保留为语法糖，删除 `structured_chat_with_messages`；新增配置 `llm_structured_mode`（`auto`/`parse`/`prompt`，默认 auto）：auto 下 parse 异常记 warning 日志并降级为 schema-in-prompt，实例级缓存 `_parse_supported=False` 避免重复试错，prompt 模式直接跳过 parse（兼容不支持 parse 的端点/本地小模型）；降级解析新增模块级纯函数 `_extract_json`（markdown 围栏剥离 + 首个 JSON 子串提取，纯 stdlib）；6 个调用点统一迁移（`_think` 与 web_filter 白得降级能力），调用方业务兜底逻辑不变；新增 `tests/test_llm_structured.py` 10 个用例。
38. ✅ **吞错点统一补 warning 级结构化日志**：按 4 组并行排查（finder/fixer 子 agent 分离），全 src/ 共甄别出 11 处「功能静默退化」吞错点并补齐结构化 warning 日志：graph.py ×4（`personality.dimension_select_failed`/`think.decision_failed`/`retrieve.memory_filter_failed`/`chat.reply_fallback`）、web_filter.py（`web.filter_doc_failed`）、page_extract.py ×2（`web.page_extract_primary_failed`/`web.page_extract_failed`）、prompt_assembly.py（`prompt.compress_failed`）、tokenizer.py（`llm.tokenize_fallback`，warn-once 防刷屏）、chroma.py（`chroma.prune_failed`）、pgembed_manager.py（`db.pgembed_stop_failed`）、cli.py（`cli.eval_score_parse_failed` + rprint 用户提示）；正常控制流（StopIteration、metadata 解析兜底、REPL 退出等）与已有日志点均未动，控制流与兜底语义零改动。补充：`web_search.py`/`search_providers.py` 的裸 `logging.getLogger` 统一为结构化 `get_logger("mr_data.online")`，11+3 处 printf 风格 warning 归并为结构化事件（`web.search_failed`/`web.search_not_configured`/`web.search_provider_unknown`/`web.search_no_results`），至此全项目日志形式统一。
39. ✅ **统一 source_type（素材来源类型）声明与实现**：问题为 4 处声明互不一致且与实际写入的第 5 套值对不上（实际值：line/event/evidence/web/dialogue）。方案为「存储侧强校验 + LLM 边界宽松」：`models/personality.py` 新增 Literal 别名 `PersonalitySourceType`（line/event/evidence，Chroma personality 集合）、`MemorySourceType`（web/dialogue，memories 集合）、`DialogueVectorRefSourceType`（line/event/evidence/web，PG `dialogue_vector_refs` 表）作为唯一事实来源，并从 `models/__init__` 导出；`PersonalityEvent.source_type`（删除从不使用的 `'web'`）与 `DialogueVectorRef.source_type`（补上漏掉的 `'evidence'`）改用 Literal 强校验，非规范值在模型构造时即报错；`ReplyReference.source_type` 保持自由 `str`（LLM 输出边界，值本来就会被 graph.py 的 `known_refs` 按检索结果覆盖，强校验会让小模型不规范输出导致整个回复解析失败），description 改为真实语义；graph.py 两处 known_refs fallback 从非规范值 `"personality"`/`"memory"` 改为 `"line"`/`"dialogue"`，回复生成 prompt 的 source_type 示例从 `web|personality|memory` 改为与素材列表实际标注一致的 `line/evidence/event/web/dialogue`；`doc/database-design.md` 新增「素材来源类型（source_type）约定」一节（5 个规范值的含义/写入路径/存储位置、按集合分组规则、读取路径、新增写入路径必须复用 Literal 别名）；新增 `tests/test_source_type.py` 4 用例锁定值域防回归，test_e2e 两处非规范 `"memory"` 用法对齐为 `"web"`/`"dialogue"`。
40. ✅ **测试 Chroma 改内存模式提速**：profile（临时插件聚合 fixture/相位耗时，跑完即删）定位最大耗时项为 `chroma_store` fixture——PersistentClient 每次初始化 ~1s × 26 次 = 26.4s，call 相位大头也是 Chroma 落盘（add ~370ms/条）。内存评估：每个测试的 chroma 目录仅 252K（多为 SQLite 固定开销，数据本体 KB 级，8 维假 embedding），远低于 100M 阈值，判定可用内存模式。实现：`ChromaStore.__init__` 新增 `ephemeral=False` 参数——内存模式下用 `chromadb.EphemeralClient()`、`persist_dir=None`、跳过 mkdir，生产默认行为不变；因 chromadb 的 EphemeralClient 是进程级单例（SharedSystemClient，同进程多实例共享存储，曾导致 `test_dialogue_memories_recall_count` 跨测试污染），init 时先删除遗留的 personality/memories 集合保证隔离。`tests/conftest.py` 的 `chroma_store` fixture 与 `test_embeddings.py` 4 处直建改用 ephemeral。效果：全量 89s→58s（-35%），maxRSS 352MB→307MB，78 passed, 1 skipped 不变。顺带回答：pgembed 11s 为每次全量跑仅 1 次的 session 级冷启动（initdb），非多次启动。
41. ✅ **测试 pgembed 集群跨运行复用（跳过重复 initdb）**：`tests/conftest.py` 的 `pgembed_server` fixture 从「每次 `tempfile.mkdtemp` 全新目录 + initdb（实测 11-15s/次）」改为固定目录 `/tmp/mr-data-pgembed-test-<uid>` 跨运行复用（复用时启动仅 ~0.14s）。两层兜底：① schema 指纹——对 `postgres.py` 模块级 `SCHEMA_SQL` 做 sha256 写入集群目录标记文件，每次启动比对，不一致（表结构变更）自动删目录重建；② 启动失败（上次崩溃残留 postmaster 锁/目录损坏）删目录重试一次。数据隔离不依赖目录新旧：既有 `reset_pg_state` 每用例 TRUNCATE 8 表 + seed 已覆盖。已知限制：同一时间只能跑一份 pytest（共享集群目录有 postmaster 锁），与 pytest-xdist 并行方向互斥（当前无并行需求）。仅改测试基建，业务代码零改动。实测四轮：冷启动 61.5s → 复用 46.5s/39.7s，篡改指纹后自动重建 53.1s，均 78 passed, 1 skipped。累计：基线 89s → 内存 Chroma 58s → 复用 PG ~40-46s（-50%+）。
42. ✅ **Chroma 写入路径幂等性统一**：三条写入路径幂等性不一致（web 资料靠 URL sha256+upsert 幂等，personality 与对话分块每次 uuid4 等于去重失效）。方案（经确认）：稳定 id 内容哈希派生 + 「已存在则跳过」语义。实现：①`db/chroma.py` 新增模块级 id 派生函数 `_stable_doc_id`/`evidence_doc_id`/`event_doc_id`/`line_doc_id`/`dialogue_chunk_memory_id`（天然键+内容的 sha256，`{kind}:{digest}` 前缀防跨类型碰撞，规则集中一处）；`add_personality_event`/`add_memory` 在调用方提供 id 时先 `get()` 判存，已存在直接返回不写入（避免 `.add()` 重复 id 抛错，且保留 `recall_count`/`added_at` 不被重放重置）。②`offline/attribution.py` 三处写入传稳定 id：evidence=`(目标日志id,维度id,片段)`、event_summary=`(目标日志id,维度id,摘要)`、对话分块=`(session_id,首/末日志id,内容)`。③`cli.py` ingest 台词传 `line_doc_id`，重复执行不再全量重复入库。④`_stable_web_id` 两洞修复：洞 1 增加 title 兜底（无 URL 无正文但有标题时不再退化 uuid4）；洞 2 新增 `_is_redirect_url` 识别 `baidu.com/link`、`so.com/link` 跳转域名，此类结果 id 退化为 `title+body` 内容键，同一页面经不同跳转链接搜索不再重复入库（零额外网络请求；残留限制：同页不同摘要仍可能重复）。⑤`doc/database-design.md` 新增「Chroma 写入幂等约定」章节。新增 `tests/test_chroma_idempotency.py` 10 用例（派生函数确定性、跳过语义、recall_count 保持、`_apply` 重放不重复、`_stable_web_id` 各分支），全量 88 passed, 1 skipped。范围外建议（未改）：PG 侧归因计数（`update_dimension` 累加、`adjustment_logs` 插入）在 `mark_dialogue_processed` 之前执行且非幂等，`_apply` 中途崩溃重跑 PG 侧会翻倍（Chroma 侧已不会重复）；event_summary 无 PG 记录属未来项 5。
43. ✅ **离线归因 PG 侧会话级事务（崩溃重放幂等）**：问题为 `_apply` 的 PG 写（`update_dimension` 计数累加、`insert_adjustment`、`insert_dialogue_vector_refs`）各自独立 commit，且 `mark_dialogue_processed` 在其后逐条执行，中途崩溃重跑导致计数翻倍、审计双份（Chroma 侧改进 42 已幂等）。实现：①`db/postgres.py` 新增实例级 `self._tx_conn` 与 `transaction()` 上下文管理器（嵌套事务 raise），事务态下 `_cursor` 读写均复用同一事务连接并抑制逐方法 commit/close（保证 `get_dimension` 读自己刚写的计数判断失活阈值），非事务态行为逐字节不变（在线路径/CLI/现有测试零影响）；②`offline/attribution.py` 的 `run()` 将 `_apply` + `mark_dialogue_processed` 循环整体包入事务，LLM 调用 `_attribute_session` 保持事务外；事务内任意异常整体回滚，会话保持 unprocessed 留待重跑。跨库一致性语义（已写入 doc/database-design.md）：Chroma 写不可回滚，崩溃后可能留孤儿向量文档，但 doc id 为确定性哈希，重跑幂等跳过/自愈。新增 `tests/test_attribution_transaction.py` 4 用例（_apply 中途两处崩溃点回滚+重跑不翻倍、非事务态 autocommit 回归、嵌套事务拒绝）。范围外建议（未改）：在线 `_log_dialogue` 多步独立 commit 同样存在半失败窗口，可复用本次事务机制。
44. ✅ **web 提取成功后真实 URL 重定 id（方案 D，洞 2 残留收口）**：问题为百度/360 跳转链接的 doc id 退化为 `sha256(title+body)` 内容键，同页两次搜索摘要 body 不同 → id 不同 → 重复入库且无法关联。实现：①`online/page_extract.py` 重构为「先统一抓取、再两级解析」——新增 `_fetch`（requests 默认跟随 302，`response.url` 即真实 URL，返回 `(html, final_url)`，失败记 `web.page_fetch_failed`），`_extract_with_trafilatura`/`_extract_with_fallback` 改为接收 HTML 纯解析；`extract` 返回新增 dataclass `ExtractedPage(text, url)`；②`graph.py` `_extract_web_pages` 提取成功后用真实 URL 重算 `new_doc["id"] = _stable_web_id(real_url)`（真实 URL 走裸 sha256 分支）、`metadata.url` 换真实 URL、原跳转链接保留 `metadata.source_url` 备查；下游 `_filter_web_docs`/`_assemble_and_generate`(known_refs)/`_log_dialogue`(vector_refs+upsert_memory) 全在提取节点后消费 doc id，零改动自动一致。已知边界：只对被提取页面生效（`web_extract_max_pages` 限制），未提取的仍按内容键。`tests/test_page_extract.py` 适配新返回结构并新增 302 跳转用例（本地 HTTPServer）；新增 `tests/test_web_reid.py` 2 用例（重定 id/metadata 断言、提取失败原文保留）。全量回归 95 passed, 1 skipped（基线 88 → +7）。
45. ✅ **在线 `_log_dialogue` 事务化**：问题为 4 步 PG 写（`insert_dialogue` user/assistant、`insert_dialogue_dimension_refs`、`insert_dialogue_vector_refs`）各自独立 commit，中途失败留脏数据（孤儿对话行/无对话的引用行）。复用改进 43 的 `pg.transaction()` 机制包入会话级事务（同事务连接内 RETURNING 的 assistant_log_id 立即可见）；Chroma `upsert_memory` 循环留事务外、PG 提交之后（不可回滚但 upsert+稳定 id 幂等）。新增 `tests/test_log_dialogue_transaction.py` 2 用例（vector_refs 失败整体回滚无残留、正常路径全部行齐全）。
46. ✅ **config 路径锚定项目根 + 数据目录配置项**：问题为 `.env`、`./data/...`、`./logs` 默认值相对 CWD，从别的目录运行会漂移甚至静默丢 .env 配置。实现：`config.py` 新增 `_find_project_root()`（从 `__file__` 向上探测含 `pyproject.toml` 的目录，找不到回退 `Path(__file__).resolve().parents[2]`，不用 CWD 保证任何运行方式下确定）；`env_file` 锚定 `<root>/.env`；新增 `data_dir` 配置（env `MR_DATA_DATA_DIR`，默认 `<root>/data`）；`pgembed_data_dir`/`chroma_persist_dir`/`personality_file`/`log_dir` 默认值改 None，由 `model_validator(mode="after")` 派生填充（显式设置含 env 优先、相对值仍相对 CWD），消费点零改动；`.env.example` 同步注释。新增 `tests/test_config_paths.py` 5 用例（锚定与 CWD 无关、data_dir 传播、显式覆盖优先、相对值保留、root 探测）。
47. ✅ **Chroma 重建防丢数据（导出备份 + 报告 + 恢复 CLI）**：问题为 `chroma_recreate_on_mismatch=True` 时 `_ensure_collection` 直接 `delete_collection` 数据全丢。实现：①`db/chroma.py` 新增 `_export_collection_backup`——删除前把 `documents+metadatas` 导出为 `<persist_dir父目录>/chroma-backups/<集合名>-<UTC时间戳>.json`（不导 embeddings，模型/维度已变更旧向量无意义，恢复时重算；空集合正常导出 count=0；ephemeral 模式跳过）；**导出失败抛 RuntimeError 阻止删除**（宁缺毋滥）；重建后 print+日志报告备份路径/条数/恢复命令提示（`chroma.backup_exported`/`chroma.recreate`）。②新增 `ChromaStore.restore_collection_backup(backup_path)`：按 collection 选对应 embedding_fn 对 document 原样重算向量（embeddings.py 的 fn 不加前缀，前缀由写入方拼好存于 document，无双重前缀问题），直调 `collection.add` 保真 metadata 运行态字段，已存在 id 跳过（幂等）。③CLI 新增 `mr-data chroma-restore <backup.json> [--force]`（目标集合非空需确认，沿用 identity delete 风格），`_print_chat_help` 同步。新增 `tests/test_chroma_backup.py` 4 用例（mismatch 先导出后重建、导出失败阻止删除、恢复重算 embedding 且幂等、ephemeral 跳过）。全量回归 106 passed, 1 skipped（基线 95 → +11）。
48. ✅ **Prompt 预算分配 priority 梯度生效（贪心注水）**：问题为 `PromptSection.priority` 设计意图是压缩预算不足时的牺牲顺序，但 `prompt_assembly.py` `_compress_to_budget` 纯按体积等比分摊预算（`target = budget × 自身token/池总token`），graph.py 声明的梯度（system/format 100 > identity 90 > user_input 80 > personality 70 > memory 60 > messages 50 > web 40）在压缩时完全失效——体积最大的 web 反而拿走最多预算，违反「人格优先」；附带问题：极端超预算时 must_keep 内部也被等比压缩。实现：`_compress_to_budget` 超预算分支改为**按 priority 降序分层贪心注水**——同优先级为一组（组内保持声明顺序），高优先级组整组装得下则原文保留并扣减剩余预算；第一个装不下的组按体积分摊剩余预算（沿用原等比公式 + `_compress_text`），之后剩余预算归零；预算耗尽的低优先级组输出占位符 `（{name} 因上下文限制已省略）`；`total <= budget` 快路径逐字节不变。must_keep 极端超预算分支（must_total >= limit）复用同一函数，自动变为 100→90→80 降序注水，附带问题同步解决。`fit_sections` 分支结构、>=80 阈值、graph.py priority 声明、`_compress_text` 均未改。新增 `tests/test_prompt_assembly.py` 5 用例（未超限原文返回、梯度下高优先级原文保留低优先级压缩、同优先级按体积分摊、must_keep 极端超预算内部梯度、预算耗尽占位符）。全量回归 111 passed, 1 skipped（基线 106 → +5）。范围外未改：79/81 阈值悬崖结构本身保留（must_keep 语义是设计意图）。
49. ✅ **落库 metadata 全面 schema 类化（存储边界全覆盖）**：问题为 `DialogueLog.metadata: Optional[dict]` 及 Chroma 两处落库 metadata 均为裸 dict 无 schema。实现（`models/personality.py` 新增 4 类，`models/__init__` 导出）：①`DialogueLogMetadata`（`inner_monologue` + `blocks: list[ReplyBlock]`），`DialogueLog.metadata` 类型收紧；`insert_dialogue` 改 `model_dump(mode="json")` 序列化；`_log_dialogue` 构造模型、`_load_recent_messages` 直接用 `meta.inner_monologue`/`meta.blocks`（删掉手工重建 ReplyBlock/ReplyReference 循环）；历史遗留行的顶层 `references` 等多余 key 由 pydantic 默认忽略不炸库。②`PersonalityDocMetadata`（personality 集合 doc）：`to_chroma_metadata()`/`from_chroma_metadata()` 集中 dimension_ids 的 CSV↔list[int] 转换，`add_personality_event`/`query_personality`/`get_personality_event_ids_by_dimension` 三处共用（返回 doc dict 形态不变）。③`DialogueMemoryMetadata`/`WebMemoryMetadata`（memories 集合两套形态）：`add_memory`/`upsert_memory` 参数类型收紧为二者的 Union，内部 `model_dump(exclude_none=True)`（Chroma 拒绝 None 值），字段默认值与旧裸 dict 逐项对齐保证落库 key 集合不变；`_persist_session_memories` 与 web 资料落库改用模型。测试：新增 `tests/test_metadata_schema.py`（PG 往返保真、遗留行容错、PersonalityDocMetadata 往返、落库 key 集回归）；`test_e2e` 的 `test_dialogue_log_metadata_roundtrip` 从陈旧顶层 `references` 形态改写为现行 blocks 形态，其余 5 处裸 dict 调用点改模型。`doc/database-design.md` 新增「落库 metadata schema」章节。范围外未改（已记录建议）：web doc 管道流转裸 dict、chunk dict key 名不一致、collection 配置 metadata、`increment_memory_recall` 就地 dict 修改。已知坑（写入 doc）：metadata 模型含 `session_id` 字段且「metadata 后覆盖」语义，调用方忘传会静默覆盖位置参数。
50. ✅ **event_summary 写入 PG adjustment_logs**：问题为离线归因 LLM 产出的 `delta.event_summary` 只写 Chroma personality 集合，PG 无任何记录（Chroma 删了永久丢失）。方案（经确认，不用 dialogue_vector_refs）：`adjustment_logs` 加列 `event_summary TEXT`（CREATE TABLE + 幂等 `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`，沿用 dialogue_logs.metadata 先例；SCHEMA_SQL 指纹变化触发测试集群自动重建）；`AdjustmentLog` 模型加字段、`insert_adjustment` 加列；`_apply` 构造 AdjustmentLog 时传 `event_summary=delta.event_summary`（与 delta 一一对应，None 落 NULL）；Chroma event 写入不变。`doc/database-design.md` 列清单/ER 图/source_type 表（event 行存储列改为双写）同步。新增 `tests/test_event_summary_pg.py` 2 用例（含/不含 event_summary 的落库断言）。两项合计全量回归 118 passed, 1 skipped（基线 111 → +7）。
51. ✅ **归因上下文 `_build_context` 重构（维度分组 + 独白入对话 + 删冗余段落）**：问题为三处——①dim_text 全部活跃维度平铺，无法区分本次会话激活维度（推新性格时缺去重对照）；②向量库人格素材检索段对归因无必要；③thought 段从结构化日志读 think 事件全文，混着检索查询等噪音，只有内心独白有用且应与对话放在一起。实现：①`db/postgres.py` 新增 `list_session_dimension_ids(session_id)`（dialogue_dimension_refs JOIN dialogue_logs DISTINCT），数据源为在线每轮实际选中维度（改进 34）；`_build_context` 按激活集合把维度分两组渲染（「本次会话中实际激活」/「其余活跃（新增维度前请先对照去重）」，空组占位）；②删除 `query_personality` 调用与人格素材整段；③`_build_transcript` 的 assistant 行并入 PG `metadata.inner_monologue`（`assistant（内心独白：…）: …`，按轮对齐、不依赖日志文件），删除 `_load_session_thoughts` 方法、thought 段落与 `read_session_events` import；`_build_context` 签名收窄为 `(session_id)`；system prompt 同步（维度分组说明 + 「内心独白原文」措辞）。`log_dir` 构造参数保留兼容（沦为死参数，记入范围外建议）。测试：新增 `tests/test_attribution_context.py` 4 用例（分组断言+移除段落断言、无 refs 会话全进其余组、独白入 transcript、`list_session_dimension_ids` 跨会话隔离）；`test_e2e.py::test_offline_attribution` 删失效的 think 日志播种块，改为 assistant 日志写 `DialogueLogMetadata(inner_monologue=...)`（顺带清理不再使用的 json/Path import）。全量回归 122 passed, 1 skipped（基线 118 → +4）。
52. ✅ **删除 `log_dir` 死参数与 `read_session_events`（改进 51 收尾清理）**：`AttributionEngine.__init__` 删除 `log_dir` 参数与 `self.log_dir`（改进 51 后仅兼容旧调用点，生产唯一构造点 cli.py 本就没传）；`logging.py` 删除无调用方的 `read_session_events` 函数及随之失效的 `typing.Any/Optional` import（`json` 仍被 `_JSONLFormatter` 使用保留）；测试侧 8 处 `AttributionEngine(...)` 调用去掉 `log_dir=` 实参，test_attribution_failure / test_attribution_context 两个 `_make_engine` helper 去掉死参数；`temp_log_dir` fixture 全部保留（仍负责把 `settings.log_dir` 重定向到 tmp，engine logger 仍在写日志）。纯删除零行为变化，全量回归 122 passed, 1 skipped 不变。
53. ✅ **归因 prompt 增强（新维度字段澄清 + LLM 去重判断 + 收敛 3 条强关联）**：问题为——①`DimensionDelta.description` 命名不清晰，system prompt 未要求 LLM 给出「新增理由」；②`_apply` 中 `insert_dimension` 无条件直接入库，LLM 新建维度可能与既有维度语义重合，有膨胀风险；③归因输出无数量约束，易牵强。实现（attribution.py）：①`description` 改名 `new_dimension_description`（第一人称描述性自白，与 dimension_id 二选一），新增 `new_dimension_reason`（新建必填：为什么既有维度无法涵盖，须引用证据；仅进 prompt 与 `insert_dimension` 处 info 日志 details，不动 PG schema，不复用 AdjustmentLog.reason）；system prompt 第 2 条改写（新建前必须逐条对照「其余活跃的性格维度」，拿不准归入既有），新增第 7 条收敛要求（最多 3 条、按关联强度降序、牵强不输出），JSON 示例同步。②`models/personality.py` 新增 `DimensionDedupMatch`（candidate_index + matched_dimension_id 可空）/ `DimensionDedupResult`（matches 全默认字段，FakeLLM 默认分支也能安全 model_validate）；`AttributionEngine._dedup_new_dimensions`（在 `_attribute_session` 返回前、`pg.transaction()` 之外调用）：收集 `dimension_id is None and new_dimension_description` 候选，`chat_structured(temperature=0.0)` 批量一次提交全部候选 + 全量活跃维度（沿用 _build_context 渲染格式），命中且 id 在活跃白名单内才改挂既有维度（改写 dimension_id、清空 new_dimension_*，记 offline.dimension_dedup_merged 日志）；幻觉 id 记 warning 忽略；调用异常降级为允许新建（offline.dimension_dedup_failed），对齐 _filter_memory_docs/filter_web_docs 的「保守原样」先例；去重后 `parsed.deltas[:offline_max_deltas_per_session]` 硬截断（先去重后截断，不按 delta 值排序——计数不是关联强度代理）。③config.py 新增 `enable_dimension_dedup: bool = True`、`offline_max_deltas_per_session: int = 3`（对齐原则 4 可配置可降级）。测试：conftest FakeLLM 加 DimensionDedupResult 分支（无需动 chat，FakeLLM 覆盖 structured_chat 不走真实降级路径）；新增 `tests/test_attribution_dedup.py` 6 用例（不重合新建、重合改挂不新增、去重异常降级、幻觉 id 忽略、开关关闭跳过、4 条截断为 3）——顺带补齐 `_apply` 新维度 insert 路径此前的零覆盖。范围外未改（need_improve.md 保留）：target_dialogue_log_id 幻觉校验/跨 session 串号/fallback 污染路径（独立待办）。全量回归 128 passed, 1 skipped（基线 122 → +6）。
54. ✅ **LLM 幻觉 id 防护（transcript 带日志 id + 返回 id 校验 + 移除 fallback 污染）**：问题为——①`_build_transcript` 拼行不带日志 id，LLM 填 `target_dialogue_log_id` 无据可依易幻觉；②主归因返回的 `dimension_id`/`target_dialogue_log_id` 无任何校验，而 `adjustment_logs.dimension_id`/`dialogue_log_id` 均有外键（postgres.py:121,126），幻觉 id 触发 FK 违规炸掉 run() 事务段、中断整个离线批处理，跨 session 串号也无防护；③`target_log_id = delta.target_dialogue_log_id or fallback_assistant_id` 兜底造成污染——evidence/event 的 doc id 哈希原料与 source_id 依赖 target，LLM 没给 id 时变成 "None"，且 dialogue_vector_refs 跳过不写产生孤儿文档。实现（attribution.py）：①`_build_transcript` 行格式改为 `user[#12]: …` / `assistant[#13]（内心独白：…）: …`（id 紧跟角色名），system prompt 第 6 条要求 target 必须取自 transcript 中 assistant 行的 [#编号]、无明确对应留空。②新增 `_validate_delta_ids(session_id, logs, result)`（在 `_attribute_session` 内、去重与截断之后调用，事务外）：`dimension_id` 不在活跃维度白名单 → 置 None + warning（`offline.invalid_dimension_id`，delta 无 new_dimension_description 时被 _apply 自然丢弃）；`target_dialogue_log_id` 不在本会话 assistant 日志 id 集合 → 置 None + warning（`offline.invalid_target_log_id`）。③`_apply` 删除 `fallback_assistant_id` 预计算与 `or fallback`；`target_log_id = delta.target_dialogue_log_id`；evidence 块守卫改为 `evidence_snippets and target_log_id is not None`（块内 vector_refs 的恒真守卫随之简化）、event_summary 块同样加守卫，跳过时记 info 日志（`offline.evidence_skipped_no_target`）；adjustment 仍落库（dialogue_log_id=NULL，列允许）；`_build_evidence_context` 删 None 分支（调用点保证非 None），StopIteration 防御兜底保留。测试适配：conftest `FakeLLMClient` 新增可设置属性 `attribution_target_log_id`（chat 与 structured_chat 两处 AttributionResult 分支带上）——改进 54 移除 fallback 后，依赖 evidence/vector_refs 落库的旧用例必须显式指定 target：`test_e2e.py::test_offline_attribution` 与 `test_attribution_transaction.py::test_crash_on_vector_refs_rolls_back_pg_writes` 两处设置实际 assistant id（后者 `_setup_closed_session` 改为返回 assistant_id）；`test_attribution_context.py` 独白行断言对齐新格式。新增 `tests/test_attribution_id_guard.py` 6 用例（transcript 带 id、幻觉/跨会话/指向 user 行 target 置 None 且 adjustment NULL + Chroma 无文档、幻觉 dimension_id 丢弃无 FK 异常、合法 target 回归保护 evidence+refs 落库）。范围外建议（need_improve.md 保留）：run() 事务段无非幻觉异常的会话级 try/except；在线侧 DimensionSelection ids 无白名单校验。全量回归 134 passed, 1 skipped（基线 128 → +6）。
55. ✅ **离线批处理会话级容错 + 在线维度选择幻觉 id 日志**：问题为——①`run()` 里 `_apply` 的事务段（attribution.py:131-134）无 try/except，非幻觉类异常（如 DB 断连）冒泡中断整个离线批处理，后续会话全部不处理；②待改进项称在线侧 `_select_dimensions` 无白名单校验。实现：①`run()` 事务段包 try/except——`transaction()`（postgres.py:167-182）异常时本就连带回滚并清理 `_tx_conn`，catch 后记 warning（`offline.apply_failed`）、`failed_sessions += 1`、`continue`；会话保持 unprocessed 下次 run() 重试，Chroma 已写文档靠确定性 doc id 重放幂等自愈（改进 43 语义）；`offline.completed` details 新增 `failed_sessions` 计数。语义变化：`run()` 不再向调用方传播单会话 apply 异常（与 LLM 阶段 `offline.session_failed` 对齐）。②经核实白名单过滤**已存在**（graph.py:183-186 `[x for x in selection.dimension_ids if x in valid_ids]`），幻觉 id 本就被静默丢弃；仅补 dropped 非空时的 warning（`personality.dimension_select_invalid_ids`，details 含 dropped_ids），过滤与全幻觉回退全量维度的语义零改动。测试：`test_attribution_transaction.py` 两个既有崩溃用例去 `pytest.raises` 改为断言 run() 正常返回 + 回滚状态不变（新语义适配）；新增 `test_failed_session_does_not_abort_batch`（两 closed 会话，会话 A 的 insert_adjustment 崩 → B 正常处理完成、A 回滚留待重跑、恢复后重跑只应用一次）；`test_graph_state.py` 新增 `test_select_dimensions_drops_hallucinated_ids`（DimensionSelection 返回 [1, 999] → 过滤为 [1]）。范围外未改：transaction() 回滚语义、全幻觉时下游回退全量维度语义、Chroma 孤儿文档进一步清理机制。全量回归 136 passed, 1 skipped（基线 134 → +2）。

56. ✅ **用户打分闭环（退出时会话级评分 → 离线归因参考）**：问题为——①评分采集是 `--eval` 逐轮询问（-1/0/1 写入 dialogue_logs.evaluation_score/feedback），打断每轮对话且归因从不读取（G2/G8 review 均指出此脱钩）；②会话级整体满意度无采集入口。实现：①存储端（postgres.py / models/personality.py）：sessions 表新增 `rating INTEGER` / `rating_comment TEXT`（幂等 ALTER TABLE 先例，SCHEMA_SQL 指纹变化触发测试集群自动重建）；`Session` 模型加 `rating`/`rating_comment` 字段；新增 `update_session_rating(session_id, rating, comment)`；`create_session` 的 `ON CONFLICT DO UPDATE` 重激活分支追加 `rating = NULL, rating_comment = NULL`（会话 id 复用为全新对话，清掉旧评分）。②采集端（cli.py）：新增模块级 `_ask_session_rating(pg, session_id)`——评分提示 `-2 很差 / -1 差 / 0 一般 / 1 好 / 2 很好（回车跳过）`，空输入直接返回；非整数或越界记 warning（`cli.session_rating_invalid`）+ 黄字提示按跳过处理；有效评分再问可选评论并落库（空评论存 NULL）。调用点两处：`/newsession` 分支 close 前、`finally` 块 close 前（后者包 try/except (EOFError, KeyboardInterrupt) 防 Ctrl+C 后二次中断崩退）。移除 `--eval` 选项、eval_mode 逐轮询问块、帮助文本 `--eval` 行（补退出评分说明）；删除随之无调用方的 `update_evaluation`（改进 52 死代码清理同例）；dialogue_logs.evaluation_score/feedback 列保留但不再采集（范围外建议：后续随 schema 清理移除）。③使用端（attribution.py）：`_build_context` 末尾追加「用户对本会话的评价」段——`get_session` 取评分，渲染 `用户评分：-1（差）` + 可选 `用户评论：…`（模块级 `_RATING_LABELS` 文案映射），无评分整段省略不污染 prompt；system prompt 新增条款：用户评分与评论是最直接的成败信号——低分/批评性评论更严格归因失败维度、评论点名性格表现优先对照既有维度、高分且肯定时强化成功计数、无评价按对话内容自行判断。④README.md `--eval` 说明更新为退出评分机制。测试：新增 `tests/test_session_rating.py` 6 用例（update/get_session 往返、复用重置、context 含评分与评论、无评分省略、CLI helper 合法输入落库、空/非法/越界输入跳过不写库——monkeypatch `Prompt.ask`）。全量回归 142 passed, 1 skipped（基线 136 → +6）。

57. ✅ **清理 `dialogue_logs` 逐轮评分残留列（`evaluation_score` / `evaluation_feedback`）**：背景为改进 56 移除 `--eval` 逐轮评分后，这两列只写 NULL、无任何读取方（G2/G8 review 早前即指出其只写不读、与归因脱钩），全项目仅剩 4 处引用。实现（用户明确指示直接清理，接受删列丢历史数据）：①`postgres.py`——CREATE TABLE `dialogue_logs` 定义删两列；既有 `ADD COLUMN IF NOT EXISTS metadata` 旁追加 `DROP COLUMN IF EXISTS evaluation_score / evaluation_feedback` 幂等迁移（已有开发库 `init_schema` 时同步清掉；SCHEMA_SQL 指纹变化触发测试集群自动重建）；`insert_dialogue` 的 INSERT 列清单与 values 元组删两列。②`models/personality.py`——`DialogueLog` 删 `evaluation_score`/`evaluation_feedback` 字段。③`tests/test_e2e.py::test_offline_attribution`——DialogueLog 构造删两个 kwargs。④`doc/database-design.md` 同步：`dialogue_logs` 字段表与 ER 图删两行；「数据流」第 3 条「评估反馈」改写为退出评分写 `sessions.rating/rating_comment`、离线归因参考；顺带补齐改进 56 的文档缺口——`sessions` 字段表与 ER 图 sessions 块补上 `rating`/`rating_comment`。纯删除零行为变化，无新增用例，全量回归 142 passed, 1 skipped 不变。
